defaults: &defaults
  REQUEST_LIMIT_MAILER: 'develop@digitalnz.org'
  HTTP_HOST: 'http://test.host'
  MONGOID_HOSTS: 'localhost:27017'

development:
  <<: *defaults

test:
  <<: *defaults

staging:
  <<: *defaults

production:
  <<: *defaults