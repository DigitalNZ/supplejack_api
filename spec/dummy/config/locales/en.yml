

# Sample localization file for English. Add more files in this directory for other locales.
# See https://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  users:
    reached_limit: You have reached your maximum number of api requests today
    anonymous_reached_limit: The anonymous key has reached the maximum number of API requests for today
    invalid_token: Invalid API Key
    blank_token: Please provide a API Key
    title: Users
    username: Username
    name: Name
    authentication_token: API Key
    role: Role
    daily_requests: API requests today
    monthly_requests: API requests last 30 days
    log_out: Log out
    edit: Edit

  site_activities:
    title: Usage by date
    
  custom_searches:
    errors:
      uniqueness: is already taken, please choose a different one

  admin:
    title: Supplejack API
    download_csv: Download as CSV
    not_a_admin: This area is restricted to Administrators

  mongoid:
    models:
      user_set: Set
    attributes:
      user_set:
        name: Name
        description: Description
        privacy: Privacy
    errors:
      messages:
        blank: "%{attribute} field can't be blank."
        inclusion: "%{value} is not a valid %{attribute} value"

  errors:
    param_missing: "Mandatory Parameter %{param} missing in request"
    record_not_found: "Record with ID %{id} was not found"
    requires_admin_privileges: "You need Administrator privileges to perform this request"
    requires_harvest_privileges: "You need Harvester privileges to perform this request.\
                                 Your API key role must have the attribute { harvester: true }.\
                                 Check the available roles in your record_schema.rb file."
    solr_unavailable: "Solr is temporarily unavailable please try again in a few seconds"
    time_out: "Request timed out"
    user_not_found: "User with provided Api Key %{key} not found"
    user_with_id_not_found: "User with provided user id %{id} not found"
    user_key_missing: 'Mandatory parameter user_key missing'
    prevent_anonymous: "Anonymous request is not allowed"

  time:
    formats:
      default: "%Y-%m-%d %H:%M:%S %Z"
      harvester: "%Y-%m-%dT%H:%M:%SZ"
      date: "%d %b %Y"
      long: "%d %b %Y at %H"
      medium: "%d %b %Y"

  date:
    formats:
      default: "%d %b %Y"
  
