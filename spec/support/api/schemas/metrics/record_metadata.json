{
  "description": "Contains metadata about the records contained in a display_collection on a specific day",
  "type": "object",
  "required": [
    "day",
    "category_counts",
    "copyright_counts"
  ],
  "properties": {
    "day": {
      "description": "Day this metric represents",
      "type": "string",
      "format": "date"
    },
    "category_counts": {
      "description": "Mapping of categoies to counts, object shape is dynamic so it has no schema",
      "type": "object"
    },
    "copyright_counts": {
      "description": "Mapping of copyrights to counts, object shape is dynamic so it has no schema",
      "type": "object"
    }
  }
}
